# -*- mode: snippet -*-
# name: cp
# key: cp
# --
#include <iostream>
#include <algorithm>
#include <functional>
#include <string>
#include <vector>
#include <utility>
#include <cmath>
#define REP(i, n) for(int i = 0; i < n; i++)
#define SORT(x) sort(x.begin(), x.end())
#define RSORT(x) sort(x.begin(), x.end(), std::greater<int>())
#define swap(a, b) \
  do { typeof(a) __tmp = (a); (a) = (b); (b) = __tmp; } while (0)
  #define vi vector<int>
  using namespace std;

const double EPS = 1e-10;
const double PI  = acos(-1.0);

int main() {
    $0
}
